import { Header } from '../../widgets'

const Cart = () => (
  <div className="flex flex-col min-h-screen">
    <Header />

    <main className="wrapper | grow py-4">
      <p>Cart page</p>
    </main>
  </div>
)

export default Cart
